<%= form_for(@vote_method) do |f| %>
  <% if @vote_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_method.errors.count, "error") %> prohibited this vote_method from being saved:</h2>

      <ul>
      <% @vote_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
